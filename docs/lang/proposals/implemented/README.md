# Implemented proposals

This directory contains proposal documents for language features that have been implemented.

Once a feature reaches this folder, the proposal is treated as historical design context rather
than the active place to evolve the feature. Further language changes should start with the
specification and, if needed, a new proposal that describes the next change explicitly.
