# Proposal: New extension syntax

To put the extended type before the extension class name.

## Summary

```raven
extension for Foo as FooExtensions {
    public Test(value: int) -> int { ... }
}
```
