import System.Console.*
import System.Threading.Tasks.*

let compute = async (value: int) -> Task<int> {
    await Task.Delay(5)
    return value
}

let result = await compute(42)

WriteLine(result)
return result
