import System.*
import System.Threading.Tasks.*

val foo = Foo()
await foo.Bar(42)

class Foo {
    async Bar(x: int?) -> Task {
        await Task.Delay(500)
        Console.WriteLine("Value: $x")
    }
}