import System.Console.*
import System.Collections.Generic.*
import System.Linq.*

let items = test()

for item in items.Where(x => x > 2) {
    WriteLine(item)
}

func test() -> IEnumerable<int> {
    var i = 1
    yield return 42
    while i < 10 {
        yield return i
        i = i + 2
    }
}