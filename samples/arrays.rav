/*
 * Array type declaration
 * * * */

import System.*
import System.Text.*

val list = [1, 42, 3]

Console.WriteLine(list.Length)

var i = 0

while i < list.Length {
    val item = list[i]
    Console.WriteLine(item)
    i = i + 1
}