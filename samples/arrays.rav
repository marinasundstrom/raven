/*
 * Array type declaration
 * * * */

import System.*
import System.Text.*

let list = [1, 42, 3]

Console.WriteLine(list.Length)

var i = 0

while i < list.Length {
    let item = list[i]
    Console.WriteLine(item)
    i = i + 1
}