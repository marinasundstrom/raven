import System.*
import System.Console.*
import System.IO.*
import System.Threading.Tasks.*

// Feature: async file write/read operations.
let tempFile = Path.GetTempFileName()
let sample = ["alpha", "beta", "gamma"]

await File.WriteAllLinesAsync(tempFile, sample)
let file = await File.ReadAllLinesAsync(tempFile)

await Task.Delay(100)

for line in file {
    WriteLine(line)
}

File.Delete(tempFile)
