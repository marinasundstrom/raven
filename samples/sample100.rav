import System.Console.*

Write("Are you ready? (yes or no): ")
val input = ReadLine()

val response = input match {
    "yes" | "y" => "Great!"
    "no" | "n" => "I'll wait for you!"
    string? x => "What did you say?"
}

WriteLine(response)