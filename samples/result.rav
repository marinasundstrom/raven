/*
 * Demonstrates discriminated unions in Raven.
 */

import System.*

val test: Result<int> = .Ok(42)

Console.WriteLine(test)

val x = test match {
    .Ok(v) => v.ToString()
    .Error(msg) => msg 
}

Console.WriteLine(x.GetType())

/*
union Result<T> {
    Ok(value: T)
    Error(message: string)
}
*/