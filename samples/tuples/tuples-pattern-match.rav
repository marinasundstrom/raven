import System.Console.*

// Feature: tuple pattern matching (advanced form).
let customerTuple: (id: int, tier: string) = (42, "Gold")

let result = match customerTuple {
    (42, let tier) => "Priority customer: ${tier}"
    (let id, let tier) => "Customer ${id}: ${tier}"
}

WriteLine(result)
