import System.*

// Feature: returning named tuple values from functions.
let person = getPerson(name: "Bob", age: 40)

Console.WriteLine("${person.name}")

func getPerson(name: string, age: int) -> (name: string, age: int)  {
    return (name, age)
}
