import System.*

// Feature: interface implementation with `use` disposal.
use foo = Foo()
foo.Do()

class Foo : IDisposable {
    val x = 1
    
    public init() => Console.WriteLine("Init")

    public func Do() -> () => Console.WriteLine("Do")

    public func Dispose() -> () => Console.WriteLine("Dispose $x")
}
