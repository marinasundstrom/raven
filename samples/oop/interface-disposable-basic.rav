import System.*

// Feature: interface implementation with `use` disposal.
use foo = Foo()
foo.Do()

class Foo : IDisposable {
    val x = 1
    
    init() => Console.WriteLine("Init")

    func Do() -> () => Console.WriteLine("Do")

    func Dispose() -> () => Console.WriteLine("Dispose $x")
}
