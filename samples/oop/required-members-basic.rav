import System.Console.*

// Feature: required members must be initialized.
val foo = Foo("Foo")

val foo2 = Foo2 with {
    Test = 42,
    x = 2
}

//foo2.Test = 2

record class Foo(Name: string)

class Foo2 {
    required var Test: int { get; init; }

    required var x = 2
}
