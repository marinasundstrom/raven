import System.Console.*

// Feature: required members must be initialized.
val foo = Foo("Foo")

val foo2 = Foo2 {
    Test = 42,
    x = 2
}

//foo2.Test = 2

public record class Foo(Name: string)

class Foo2 {
    public required var Test: int { get; init; }

    public required var x = 2
}
