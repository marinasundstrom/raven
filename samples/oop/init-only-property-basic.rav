
import System.Console.*

// Feature: init-only property assignment via with initializer.
val foo = Foo with {
    A = 42
}

WriteLine(foo.A)

//foo.A = 2

class Foo {
    var A: int { get; init; }
}
