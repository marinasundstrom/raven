
import System.Console.*

// Feature: init-only property assignment via an object initializer.
let foo = Foo {
    A = 42
}

WriteLine(foo.A)

//foo.A = 2

class Foo {
    var A: int { get; init; }
}
