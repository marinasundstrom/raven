import System.Console.*
import MathHelpers.*

// Feature: pipe operator with static method call.
let result = 5 |> Increment(2)

WriteLine("Result: $result")

static class MathHelpers {
    static func Increment(x: int, amount: int) -> int {
        return x + amount
    }
}
