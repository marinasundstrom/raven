import System.Console.*

val f: Foo? = Foo(21);
//val s = f?(2)
f?.Hello()

//WriteLine("Result: $s")

public class Foo(b: int) {
    public self(factor: int) -> int {
        return b * factor
    }
}

public extension Test for Foo {
    public Hello() -> string {
        return ""
    }
}