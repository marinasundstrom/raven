import System.*
import System.Console.*

val z = 2;

val a = (x: int) => x + z
val b = (x: int) -> int => x + z

val makeAdder = (x: int) -> int -> int => (a: int) => x + a + z
//val makeAdder = (x: int) -> Func<int, int> => (a: int) => x + a + z

WriteLine(makeAdder(2)(3))

WriteLine(a(3))
WriteLine(b(3))