import System.*
import System.Console.*
import System.Linq.*

let r = typeof(System.Object)
WriteLine(r)

let members = r.GetMembers()

for member in members.Where(x => x.Name.Equals("Equals")) {
    WriteLine(member.Name)
}

let member = members[1]

WriteLine(member.Name)

let method = (System.Reflection.MethodInfo)member
let type = method.Invoke(member, null)

WriteLine(type)