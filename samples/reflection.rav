import System.*
import System.Console.*
import System.Linq.*

val r = typeof(String)
WriteLine(r)

val members = r.GetMembers(.Public | .Static)

for member in members.Where(x => x.Name.Equals("Intern")) {
    WriteLine(member.Name)
}

val member = members[1]

WriteLine("First ${member.Name}")

/*
val method = (System.Reflection.MethodInfo)member
val type = method.Invoke(member, null)

WriteLine(type)
*/