import System.*
import System.Console.*
import System.Linq.*

let r = typeof(String)
WriteLine(r)

let members = r.GetMembers(.Public | .Static)

for member in members.Where(x => x.Name.Equals("Intern")) {
    WriteLine(member.Name)
}

let member = members[1]

WriteLine("First ${member.Name}")

/*
let method = (System.Reflection.MethodInfo)member
let type = method.Invoke(member, null)

WriteLine(type)
*/