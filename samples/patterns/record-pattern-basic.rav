import System.*
import System.Console.*

func Main() {
    // Feature: positional record pattern.
    val fooTrue  = Foo(true,  (10, 20))
    val fooFalse = Foo(false, (1, 2))

    test(fooTrue)
    test(fooFalse)
}

func test(x: object?) {
    if x is Foo(true, (10, val a)) {
        WriteLine("Foo with Value == true && $a")
    }
}

record class Foo(Value: bool, Data: (int, int))
