import System.*
import System.Console.*

func Main() {
    // Feature: positional record pattern.
    let fooTrue  = Foo(true,  (10, 20))
    let fooFalse = Foo(false, (1, 2))

    test(fooTrue)
    test(fooFalse)
}

func test(x: object?) {
    if x is Foo(true, (10, let a)) {
        WriteLine("Foo with Value == true && $a")
    }
}

record class Foo(Value: bool, Data: (int, int))
