import System.Console.*

// Feature: collection pattern with fixed-length destructuring.
let lineItemQuantities: int[] = [1, 2]

let totalUnits = match lineItemQuantities {
    [let first, let second] => first + second
    _ => 0
}

WriteLine(totalUnits)
