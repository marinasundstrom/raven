import System.Console.*

val value = 42

WriteLine(value.GetType().Name)

val result = value match {
    //"foo" => "it's foo"
    int x => "foo"
    //string str => str
    //object obj => "obj"
    _ => "None"
}

WriteLine(result.GetType().Name)
WriteLine(result)