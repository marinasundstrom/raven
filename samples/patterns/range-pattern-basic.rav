import System.Console.*

// Feature: range patterns over numeric and character values.
let shipmentCount = 4

let sizeBand = match shipmentCount {
    ..4 => "No"
    40 .. 43 => "Yes"
    _ => "What?"
}

WriteLine(sizeBand)

let routeCode = 'B'

let routeBand = match routeCode {
    'a' .. 'b' => "lowercase"
    'A' .. 'B' => "uppercase"
    _ => "Not in range"
}

WriteLine(routeBand)
