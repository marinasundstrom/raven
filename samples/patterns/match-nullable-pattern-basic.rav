import System.Console.*

// Feature: nullable pattern matching.
val priorityOverride: int? = null

val result = match priorityOverride {
    null => "Use default priority"
    int value => "Override priority: ${value}"
}

WriteLine(result)
