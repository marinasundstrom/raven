import System.Console.*

// Feature: basic type pattern in `match` expression.
let metricPayload: object = 42

let result = match metricPayload {
    int value => "Numeric metric: ${value}"
    _ => "Unsupported metric payload"
}

WriteLine(result)
