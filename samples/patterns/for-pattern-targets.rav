import System.Console.*

let points = [(0, 0), (1, 0), (1, 1), (2, 0)]

for (let x, 0) in points {
    WriteLine(x)
}

let words = ["raven", "lang", "rvn", "compiler"]

for [let first, ..let rest] in words {
    WriteLine(first)
    WriteLine(rest)
}
