import System.*
import System.Console.*

WriteLine(Test(1))

func Test(y: int) -> int {
    return y match {
        0 => return 0               // abrupt
        1 => 42                     // value
        _ => throw Exception("x")  // abrupt
    }
}