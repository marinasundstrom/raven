import System.*
import System.Console.*
import System.Collections.Generic.*

val arr = [1, 2, 3]

val x = arr.FirstOrError(() => DomainError.NotFound)

WriteLine(x)

union DomainError {
    NotFound
    Unexpected(exception: Exception)
}