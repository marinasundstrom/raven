import System.*
import System.Console.*
import System.Linq.*

func Main() {
    val arr1 = [1, 2, 3]
    val arr2 = [1, ...arr1, 3]

    val [a, b, ...] = arr2

    for x in [2, 1..2] {
        WriteLine(x)
    }

    for val [..2, ..2 x, ...] in [[2, 1..3]] {
        WriteLine(x)
    }
}