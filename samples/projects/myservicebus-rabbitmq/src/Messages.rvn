namespace MyServiceBusRabbitMqSample

record SubmitOrder(
    OrderId: Guid, 
    Customer: string)

record OrderSubmitted(
    OrderId: Guid, 
    AcceptedAt: DateTimeOffset)

record CheckOrderStatus(
    OrderId: Guid)

record OrderStatus(
    OrderId: Guid, 
    Status: string)
