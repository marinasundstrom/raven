import System.Console.*

func Main() {
    Write("Write a number: ")
    let input = ReadLine()!
    let Ok(no) = int.Parse(input) else {
        WriteLine("\"$input\" is not a number")
        return
    }

    WriteLine("Number is: $no")
}