import System.*
import System.Console.*
import System.Globalization.*

func Main() -> Result<(), IParseError> {
    // Custom extension
    val test = int.parse("foo")
    
    match test {
        Ok(val v) => WriteLine(v)
        Error(ParseIntError(val kind, _, _)) => {
            WriteLine(kind)
        }
    }

    WriteLine("Value: $test")

    return Ok
}