import System.*
import System.Console.*
import System.Globalization.*

func Main() -> Result<(), IError> {
    val test = int.parse("42")?

    /*
    match test {
        Ok(val v) => WriteLine(v)
        Error(ParseIntError(.InvalidFormat, _, _) => WriteLine("Ouch! Invalid format")
        _ => WriteLine("Unhandled error")
    }
    */

    WriteLine("Value: $test")

    return Ok
}