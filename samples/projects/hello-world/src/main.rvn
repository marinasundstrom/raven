import Utilities.*

func Main() {
    Test()
    val x = A(42)
    Test()
    A(x)
    A("42")

    use test = Test2()

    val v: Foo = 3
    val r = v match {
        3 => 3
        int i => i
        string => 2
        _ => 4
    }

    Console.WriteLine(r)
}

union Foo(int | string | null)