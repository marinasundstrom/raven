namespace Utilities

import System.Console.*

func Test() {
    WriteLine("Hello")
}

func A(x: int) -> int {
    42 + x
}

[Obsolete("Test")]
func A(x: string) -> int {
    42 + int.Parse(x)
}

func Test2() -> IDisposable {
    return Disposable()
}

class Disposable : IDisposable {
    // If I comment out: There is no error diagnostic telling IDisposable.Dispose() has not been implemented
    func Dispose() {}
}