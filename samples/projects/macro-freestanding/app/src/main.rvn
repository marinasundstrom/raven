import System.Console.*

func Main() -> unit {
    let answer = #add(20, Right: 22)
    let shouldRetry = #guard {
        unless answer == 42
    }

    WriteLine(answer)
    WriteLine(shouldRetry)
}
