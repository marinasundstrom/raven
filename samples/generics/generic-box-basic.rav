import System.*

// Feature: user-defined generic class.
let ints = Box<int>(1)
let words = Box<string>("ok")

Console.WriteLine(words.Value)

class Box<T>
{
    val Value: T

    init(value: T) { Value = value }
}
