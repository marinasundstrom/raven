class Foo<T> {
    class Bar {
        let value: T
    }
}

class Outer<A> {
    class Inner<B> {
        let value: A
        let b: B 
    }
}

let a = Foo<int>.Bar()
let a = Outer<int>.Inner<string>()