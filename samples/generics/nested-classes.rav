// Nested types that inherit parameters from enclosing type when instantiated.

val a = Foo<int>.Bar()
val b = Outer<int>.Inner<string>()

class Foo<T> {
    public class Bar {
        val value: T
    }
}

class Outer<A> {
    public class Inner<B> {
        val value: A
        val b: B
    }
}
