import System.*
import System.Console.*

let res = Test(null)

WriteLine(res)

func Test(name: string?) -> Result<string, CustomError> {
    let str = name ?? return Error(CustomError("Argument '${nameof(name)}' is null"))
    WriteLine(str)

    Ok(str)
}

record class CustomError(Message: string)