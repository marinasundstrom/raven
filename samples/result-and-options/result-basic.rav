import System.*

// Feature: simplest `Result<T, E>` form.
let invoiceTotal: Result<int, string> = Ok(42)

Console.WriteLine(invoiceTotal)

let label = match invoiceTotal {
    Ok(let total) => "Total: ${total}"
    Error(let message) => message
}

Console.WriteLine(label)
