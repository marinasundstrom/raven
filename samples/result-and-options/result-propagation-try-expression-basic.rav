import System.*
import System.Console.*

let a = handle("42")
Console.WriteLine(a)

func handle(text: string) -> Result<int, Exception> {
    let x = try? int.Parse(text)
    WriteLine(x)
    return Ok(x)
}