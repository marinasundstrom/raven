import System.*
import System.Console.*

// Feature: simplest `Option<T>` form + common extension methods.
let promoPercent: Option<int> = Some(10)
let fallbackPercent = promoPercent.UnwrapOr(0)

WriteLine("HasSome: ${promoPercent.HasSome}")
WriteLine("Percent: ${fallbackPercent}")
