import System.*
import System.Console.*

val a = handle("42")
Console.WriteLine(a)

func handle(text: string) -> Result<(), Exception> {
    val x = try? throwing()
    WriteLine(x)
    return Ok(x)
}

func throwing() {
    throw new InvalidOperationException("Too")
}