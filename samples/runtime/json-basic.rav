import System.Console.*
import System.Text.Json.*

let person = Person("Alice", 42)

WriteLine(JsonSerializer.Serialize(person, JsonSerializerOptions with {
    WriteIndented = true
}))

record class Person(Name: string, Age: int);
