import System.Console.*
import System.Text.Json.*

val person = Person("Alice", 42)

WriteLine(JsonSerializer.Serialize(person, JsonSerializerOptions {
    WriteIndented = true
}))

record class Person(Name: string, Age: int);