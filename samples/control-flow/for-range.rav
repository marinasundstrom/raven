import System.Console.*

// Feature: `for` loop with a range expression.
let region = "NorthHub"
let startBatchId = 1001
let endBatchId = 1005

for batchId in startBatchId..endBatchId {
    WriteLine("[${region}] Create shipment batch ${batchId}")
}
