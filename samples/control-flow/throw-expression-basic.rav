import System.*
import System.Console.*

// Feature: throw expression in null-coalescing.
try {
    Test(null)
} catch (Exception exc) {
    WriteLine(exc)
}

func Test(name: string?) {
    val x = name ?? throw ArgumentNullException(nameof(name))
    WriteLine(x)
}
