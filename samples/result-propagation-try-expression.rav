import System.*
import System.Console.*

val a = handle("42")
Console.WriteLine(a)

func handle(text: string) -> Result<int, Exception> {
    val x = try? int.Parse(text)
    WriteLine(x)
    return Ok(x)
}