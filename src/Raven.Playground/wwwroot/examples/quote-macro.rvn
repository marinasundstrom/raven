import System.Console.*
import Raven.CodeAnalysis.Syntax.*
import Raven.Macros.*

let expression: ExpressionSyntax = quote! {
    20 + 22
}

WriteLine("Quoted Raven expression:")
WriteLine(expression.ToFullString())
