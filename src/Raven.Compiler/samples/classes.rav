//namespace Test

import System

Console.WriteLine("Hello"); let x = 2

//let f = Foo(t: 42)
//let x = f.Bar(arg: 2)

class Foo {
    let a = 2
    var b : int = 2

    public init(t : int) {
        b = t
    }

    public Bar(arg : int) -> int {
        return self.x 
            + arg
    }

    public Item -> string {
        get {
            return item
        }

        set -> 2
    }
}
