import System.*

let foo = Foo()
foo.Dispose()

class Foo : IDisposable {
    public init() {
        Console.WriteLine("Init")
    }

    public Dispose() -> unit {
        Console.WriteLine("Dispose")
    }
}