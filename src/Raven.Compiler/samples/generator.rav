import System.Console.*

let items = test()

for item in items {
    WriteLine(item)
}

func test() -> IEnumerable<int> {
    let x = 0
    yield return 42
    while x < 10 {
        yield return i
        i = i + 1
    }
}