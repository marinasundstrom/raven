import System.*
import System.Threading.Tasks.*

let c = C()
let result = await c.Work()

Console.WriteLine(result)

class C {
    async Work() -> Task<string> {
        return try await Task.FromResult(1) match {
            int value => value.ToString()
            Exception ex => ex.Message
        }
    }
}