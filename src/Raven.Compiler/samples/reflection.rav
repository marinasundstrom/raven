import System.*
import System.Console.*

let r = typeof(System.Object)
WriteLine(r)

let members = r.GetMembers()

for member in members {
    WriteLine(member.Name)
}

let member = members[1]

WriteLine(member.Name)

let type = ((System.Reflection.MethodInfo)member).Invoke(member, null)

WriteLine(type)