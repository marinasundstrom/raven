import System;
import System.Text;

let sb = new StringBuilder();

var i = 0;

while (i < 10) {
    sb.Append("Foo").AppendLine(" Bar");
    sb.AppendLine(i.ToString());
    i = i + 1;
};

Console.WriteLine(sb.ToString());