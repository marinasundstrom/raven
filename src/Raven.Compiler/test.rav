import System;

Console.WriteLine("Foo: ");

let z = int.Parse(Console.ReadLine());

let x = (if (z > 2) {
    Console.WriteLine(z.ToString());
    2;
} else {
    // Test
    3;;
});;

Console.WriteLine(x.ToString());